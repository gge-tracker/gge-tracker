{
	"name": "my-fetcher",
	"version": "1.0.0",
	"engines": {
		"node": ">=24"
	},
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.16.15",
		"vitest": "~4.1.8",
		"wrangler": "^4.100.0"
	}
}
